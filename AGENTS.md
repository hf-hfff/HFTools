# 项目规则（AGENTS.md）

***

## 一、角色定位

你是一位服务于 **HFTools** 的协作型 AI 助手，需在理解项目背景的前提下，按统一规范完成任务交付。

***

## 二、项目认知

### 2.1 项目简述

- 个人vibe coding项目，主要面向找工作时作为成果演示；
- 项目主要以个人工具箱为概念，封装日常常用AI工具；

### 2.2 项目功能

工具分类与语义（均为架构设计，未实现业务）：

- 摄影：照片点评（多模态 LLM 分析）、照片AI优化（LLM 生成优化建议 + 生图模型重绘，单次限 1 张）、摄影器材参数、摄影基础知识
- AI工程：提示词工程、AI工具下载/介绍、git热点追踪（抓取 GitHub Trending 开放网页 + 联网搜索生成项目简介，支持跳转对应仓库）
- 求职：JD拆解、简历优化、公司背调（联网搜索公司公开信息 + LLM 整理分析报告，面向招聘入职方向）

优先级分期（最终以 spec 为准）：

- P0（一期 MVP）：管理平台、照片点评、照片AI优化、JD拆解、git热点追踪
- P1（二期）：简历优化、公司背调、提示词工程
- P2（最低）：摄影器材参数、摄影基础知识、AI工具下载/介绍

### 2.3 技术架构

- 部署：仅本地运行，不对外开放；docker-compose 编排应用服务；Dify 复用本地已有部署，应用经 API 调用
- 工程结构：monorepo（frontend / server-node / worker-python）
- 后端：按功能选用 Node.js 或 Python —— Node.js 作为 API 网关，承载全部面向前端的接口与 AI 调用代理；Python 承担文档解析、图片处理等生态优势任务
- 前端：Vue3 + Vite + Pinia + Vue Router，UI 组件库 Element Plus；用户端与管理平台同仓
- 数据库：SQLite（工具开关、模型配置、内容数据）
- Dify：仅部分多步骤流程使用（如简历优化），单轮调用直连 LLM API；一期将 Dify 配置纳入管理平台统一管理
- 管理平台：单管理员账号 + JWT/session 鉴权；统一配置 LLM、生图、Dify（含 base_url、API key、models）；基于工具注册机制管理各工具前端显隐；多模型切换延后
- 安全：前端零密钥，所有 AI/外部 API 调用经后端代理；API key 加密落盘
- 模型能力要求：照片点评需多模态 LLM；照片AI优化需支持图生图（img2img）的生图模型；公司背调/git热点追踪的联网搜索统一使用 Dify 内置搜索能力，不单独配置搜索 API

### 2.4 阶段成果汇总

<!--
按业务模块组织，仅保留各模块最终状态；新增模块时追加一行。
格式：- YYYY-MM-DD | <模块名> | <最终产出> | <关键指标>
-->

- 2026-09-09 | 平台框架 | monorepo（frontend/server-node/worker-python）+ Node 网关 + 工具注册机制（10 工具种子）+ 管理平台（登录/工具开关/模型配置 AES 加密）+ docker-compose | spec 验收 15/15 通过，4 次 git 提交（dc859db→013ab23）
- 2026-09-09 | 照片点评 | 复用摄影点评 skill（prompt 资产迁入 server-node/assets + 熊猫 GIF 静态服务 /static）+ POST /api/ai/photo-review 多模态 LLM 代理（OpenAI 兼容、key 解密后端调用、错误分类+上游详情透出）+ 前端点评页（上传/描述/Markdown 渲染 DOMPurify 净化）+ 工具状态 placeholder→active 迁移 | 前后端编译通过；浏览器 UI 完整路径验证通过（真实多模态 LLM 点评约 30s 返回，打分/GIF 触发/Markdown 渲染均正常）；修复前端取数 bug（res.data.review→经 ApiResponse.data）；交互打磨：点评中禁用换图、120s 超时弹窗详因、拍立得显影等待动画+轮播文案+计时、返回工具箱按钮；修复 marked 对「**加粗。**紧跟中文」不渲染的 CJK 边界问题（normalizeBold 预转 strong，node 级管线验证通过）；点评框底部新增 AI 免责提示；首页删"浏览全部工具"按钮、工具卡增加"可用"绿色实线标识（与开发中虚线灰对比）；背景照片墙：assets/bg-photos 经 import.meta.glob 自适应收集，白色相纸边框 + 错乱排布（seeded 伪随机稳定布局）+ 聚光灯轮播（3s 切换、CSS transition、尊重 prefers-reduced-motion）+ 强蒙层（0.78+vignette）保证可读性，pointer-events:none 不挡上传交互 | 浏览器五轮验证通过（10 张照片渲染、轮播 active 索引切换、dropzone 命中不受遮挡）；修复 glob 相对路径 bug（../../assets→../assets）

***

## 三、任务执行 SOP

### 3.1 接收任务

1. **识别任务类型**：需求 / 方案 / 编码 / 文档 / 数据 / 杂项
2. **确认任务归属**：是否涉及已有功能逻辑
3. **澄清不明确点**：

   - 涉及已有功能 → **直接询问**，不臆测

   - 涉及新增功能 → 确认目标、用户、场景、边界、风险、成功标准

### 3.2 执行任务

- **严格按规范操作**：禁止执行任何非规范操作（如越权删改、未授权部署等）

- **保持最小变更**：不引入与当前任务无关的"顺手优化"

- **记录关键决策**：执行过程中的重要判断需可回溯

### 3.3 交付任务

- 交付前自检：是否完成、是否破坏既有功能、是否符合规范

- **更新项目文档**：将任务结果沉淀到对应文档

- **清理临时文件**：删除调试、测试、临时生成的中间产物，保护项目数据安全

***

## 四、协作约定

### 4.1 必须先确认再执行

- 涉及**删除、覆盖、批量修改、不可逆操作**

- 涉及**外部消息发送**（邮件、通知、评论等）

- 涉及**隐私、敏感数据、密钥、账号**

- 涉及**可能产生费用或明显业务后果**的操作

### 4.2 可直接执行

- 低风险、可逆、不影响外部的读写操作

- 已有明确规范支撑的常规任务

### 4.3 假设与补充

- 不影响结果的小细节：可自行合理假设，并**显式说明假设**

- 与当前目标强相关的必要补充：可直接补齐，无需反复确认

- 明显超出当前需求范围：先确认再执行

***

## 五、文档维护规则

| 文档类型        | 维护时机     | 维护方式       |
| ----------- | -------- | ---------- |
| `AGENTS.md` | 项目规则变更时  | 直接覆盖，更新前确认 |
| 阶段成果        | 阶段性交付完成时 | 在 2.4 追加一行 |
| 任务交付物       | 任务完成后    | 沉淀到对应业务文档  |
| 临时文件        | 任务结束后    | 立即删除       |

- 本项目文件命名不需要增加`_codex`或`_trae`后缀
***

## 六、冲突处理优先级

当规则冲突时，按以下顺序执行：

1. 安全与隐私
2. 用户明确指令
3. 本项目规则
4. 通用全局规则
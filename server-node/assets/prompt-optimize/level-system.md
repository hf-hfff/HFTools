# 级别：系统提示词

本次优化对象是**系统提示词**（system prompt，定义模型的角色、能力边界与行为规范，供反复使用）。优化重点：

- 角色定义清晰：身份、专业领域、语气风格一句话立住
- 能力边界：明确该做什么、拒绝什么、超出范围时如何回应
- 行为约束：工作流程（如有）、交互规范、语气与称呼约定
- 输出规范：默认输出格式、语言、长度习惯，保持多次调用的一致性
- 结构分层：按「角色 / 能力 / 约束 / 输出规范」分节组织，便于维护与扩展

# 构建上下文为仓库根目录：排除无关内容，加速构建并避免本地状态进入镜像
.git
.gitignore
.trae
.vscode

# 依赖与构建产物（容器内重新安装构建）
**/node_modules
server-node/dist
frontend/dist

# 本地数据目录（容器内以卷挂载，不打入镜像）
data
server-node/data

# 日志与临时文件
*.log
*.tmp
